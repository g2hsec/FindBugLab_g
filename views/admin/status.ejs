<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>🛠️ 관리자 대시보드</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    pre { white-space: pre-wrap; word-break: break-word; }
    .card { box-shadow: 0 0 8px rgba(0,0,0,0.1); }
  </style>
</head>
<body class="bg-light">
  <div class="container py-4">
    <h2 class="mb-4">🛠️ 관리자 시스템 점검</h2>
    <p class="text-end">환영합니다, <strong><%= user.username %></strong>님</p>
    <a href="/" class="btn btn-outline-primary">
        🏠 메인페이지
    </a>

    <div class="row row-cols-1 row-cols-md-2 g-4">
      <div class="col">
        <div class="card border-primary">
          <div class="card-header bg-primary text-white">📈 시스템 Uptime</div>
          <div class="card-body">
            <pre><%= uptime %></pre>
          </div>
        </div>
      </div>

      <div class="col">
        <div class="card border-success">
          <div class="card-header bg-success text-white">💽 디스크 사용량</div>
          <div class="card-body">
            <pre><%= disk %></pre>
          </div>
        </div>
      </div>

      <div class="col">
        <div class="card border-warning">
          <div class="card-header bg-warning text-dark">🧠 메모리 상태</div>
          <div class="card-body">
            <pre><%= mem %></pre>
          </div>
        </div>
      </div>

      <div class="col">
        <div class="card border-info">
          <div class="card-header bg-info text-white">🌐 네트워크 테스트 (ping)</div>
          <div class="card-body">
            <pre><%= ping %></pre>
          </div>
        </div>
      </div>
    </div>

    <hr class="my-4" />
    <h5>Console Test</h5>
    <form method="POST" class="input-group mb-3">
      <input type="text" class="form-control" name="cmd" placeholder="예: ls -al /; whoami" required>
      <button class="btn btn-danger">실행</button>
    </form>

    <% if (output) { %>
      <h6>출력 결과:</h6>
      <pre class="bg-dark text-light p-3 rounded"><%= output %></pre>
    <% } %>
  </div>
</body>
</html>
